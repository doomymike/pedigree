import javax.swing.*;
import java.awt.*;

public class DisplayGeneration extends JPanel {
	public DisplayGeneration(int index) {
		// TODO: remove
		setBorder(BorderFactory.createLineBorder(Color.GREEN));
	}
}
